version: '3.9'

services:
  spring-boot:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_AUTHOR: ${APP_AUTHOR}
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/moviex.conf:/etc/nginx/nginx.conf
    depends_on:
      - spring-boot
